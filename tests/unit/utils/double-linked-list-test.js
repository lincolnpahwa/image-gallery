import doubleLinkedList from 'photo-gallery/utils/double-linked-list';
import { module, test } from 'qunit';

module('Unit | Utility | double-linked-list', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = doubleLinkedList();
    assert.ok(result);
  });
});
