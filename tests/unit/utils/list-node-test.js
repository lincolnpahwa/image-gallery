import listNode from 'photo-gallery/utils/list-node';
import { module, test } from 'qunit';

module('Unit | Utility | list-node', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = listNode();
    assert.ok(result);
  });
});
